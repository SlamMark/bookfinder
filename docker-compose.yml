services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./downloads:/app/downloads
    # Phase 2: uncomment when bot.py is ready
    # command: python bot.py
